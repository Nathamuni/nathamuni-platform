# v25 — UI polish pass (owner: "perfect UI upgrade, no limits")

Guided by ui-ux-pro-max audit. The design language (dark glass, violet/cyan) is
already coherent — this pass removes the unprofessional markers and adds the
three approved quick wins. No redesign, no new pages.

## Changes
1. Mobile tab bar: text glyphs (⌂ ▶ ✦ ☬) → inline SVG stroke icons with
   animated active state + ≥44px touch targets (top "no emoji/glyph icons" rule)
2. "Save my progress" pill: shown only on /courses + /sessions (it competes
   with the nav everywhere else; those are the only pages with progress)
3. SaveNudge added to course ActionChecklist (2+ actions checked)
4. Metric streaks: lib/streak.ts (+tests) — consecutive-day chip with flame
   SVG in MetricTracker stats
5. globals.css micro-polish: text-wrap balance (headings) / pretty (body),
   tab-bar active indicator, touch-action manipulation on tap targets

## Acceptance
- No structural glyph icons remain in primary nav
- Pill absent on all pages except /courses//sessions (and their detail pages)
- Streak chip appears only with 2+ consecutive days
- Tests (incl. updated AccountWidget/Nav ones), lint, type-check, build green
- PR → CI → merge → verify live

## Failure mode watched
usePathname in AccountWidget breaks isolated component tests → mock
next/navigation in the affected test files.
